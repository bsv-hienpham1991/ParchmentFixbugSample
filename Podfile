# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ParchmentFixbugSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ParchmentFixbugSample

  target 'ParchmentFixbugSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'Parchment'
  pod 'SnapKit', '~> 5.0.0'
end
